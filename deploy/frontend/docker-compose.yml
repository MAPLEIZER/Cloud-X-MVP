services:
  frontend:
    build:
      context: ../..
      dockerfile: Dockerfile.frontend
      args:
        FRONTEND_API_BASE_URL: ${FRONTEND_API_BASE_URL:-http://localhost:5001}
    image: cloudx-frontend:dev
    container_name: cloudx-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
